import { styled } from '@mui/material';
import Container from '@mui/material/Container';

export const ContainerUI = styled(Container)({
  display: 'flex',
  flexDirection: 'column',
  padding: 0,
  width: '100%',
  justifyContent: 'center',
  alignItems: 'center',
  overflow: 'hidden',
});
